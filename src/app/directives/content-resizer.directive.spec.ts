import { ContentResizerDirective } from './content-resizer.directive';

describe('ContentResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentResizerDirective();
    expect(directive).toBeTruthy();
  });
});
